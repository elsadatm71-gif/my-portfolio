source "https://rubygems.org"

# GitHub Pages compatible Jekyll and plugins
gem "github-pages", group: :jekyll_plugins

# Windows local preview support
platforms :mingw, :x64_mingw, :mswin do
  gem "tzinfo-data"
  gem "wdm", "~> 0.1.1"
end
